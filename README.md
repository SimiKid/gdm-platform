# GDM Platform

AI-supported group decision-making study platform (master's project).

- `apps/` — participant client, admin dashboard, control plane, rule engine
- `packages/shared` — shared TypeScript types
- `infra/` — Synapse, Postgres, Etherpad (docker-compose)
- `docs/architecture` — C4 model (Structurizr DSL)
