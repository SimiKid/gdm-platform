{
  "name": "gdm-platform",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "oxlint backend/chat-service/src backend/session-manager/src frontend/admin-dashboard/src frontend/participant/src packages/shared/src e2e",
    "test": "pnpm -r test",
    "test:cov": "pnpm -r test:cov",
    "test:integration": "pnpm -r test:integration",
    "dev": "pnpm -r --parallel dev",
    "test:e2e": "pnpm -r test:e2e"
  },
  "devDependencies": {
    "oxlint": "^1.72.0"
  }
}
